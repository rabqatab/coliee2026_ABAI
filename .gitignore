# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Data (too large for git)
data/

# Documentation & analysis (kept locally, not tracked)
docs/
notebooks/
__marimo__/
# Superpowers skills/plans — kept locally, not tracked (per user instruction)
superpowers/

# Paper (kept locally, not tracked)
paper/
# Paperwork: latest paper sources/PDFs — NEVER commit (per user instruction)
paperwork/

# Output artifacts
output/
.worktrees/

# Sensitive / personal files
*.pdf
CLAUDE.md
# Local MCP / tooling config (machine-specific paths)
.mcp.json
